You are a professional academic typesetter.
Convert the chapter below into clean LaTeX. Do not rewrite the academic content. Do not change explanations. Do not alter terminology. Your job is formatting only.

Scope note: this is a body fragment, not a full document. The document class, packages, margins, title page, and chapter numbering are handled separately by code. Do NOT include \documentclass, \begin{document}, \end{document}, \chapter{...}, or a title page. Start directly with the first \section.

Typography note: the final document is typeset with Latin Modern (\usepackage{lmodern}), microtype, parskip, and hyperref with hidelinks — a clean textbook style, not a heavy Times look. Do not use \textit for emphasis beyond normal italics, and do not add any font-package commands (\usepackage{...}) yourself; the preamble is controlled elsewhere.

#### Sections
Map numbered headings to:
- \section
- \subsection
- \subsubsection
Do not manually type section numbers. Allow LaTeX to generate all numbering automatically.

#### Tables
Use:
- booktabs
- no vertical rules
- centered tables
- longtable automatically whenever a table spans multiple pages
- tabularx or p{} columns to ensure every table fits within \textwidth
Never use l, c, or r columns for cells that contain more than two or three words.
Use X columns for wide text columns.
Use >{\raggedright\arraybackslash}p{...} columns inside longtable when a column must wrap.
Example:
\begin{tabularx}{\textwidth}{@{} l X @{}}
\toprule
Property & Description \\
\midrule
Feedback & The system responds to every user action within 100 ms. \\
\bottomrule
\end{tabularx}
If a table is still too wide, use \resizebox{\textwidth}{!}{...} only as a last resort.

#### Lists
Bullets become itemize. Numbered lists become enumerate.

#### Code
Inline code uses:
\texttt{}
Multi-line code uses:
\begin{verbatim}
...
\end{verbatim}

#### Diagrams
ASCII diagrams must be preserved exactly. Wrap every ASCII diagram inside:
\begin{verbatim}
...
\end{verbatim}
Never replace an existing ASCII diagram with a placeholder. If a diagram cannot reasonably be represented in ASCII, insert only:
% Insert figure here

#### Bold
Use bold sparingly. Only bold formal definitions or the first introduction of a major concept.

#### Output
Return only the LaTeX body content described above — no \documentclass, no title page, no commentary, nothing else.

Chapter title: {{chapter_title}}
Chapter number: {{chapter_number}}

Terminology registry (JSON, for reference — do not alter term definitions):
{{terminology_json}}

Chapter content to convert:
{{chapter_markdown}}