You are a professional academic typesetter. Convert the chapter content below into clean LaTeX BODY content only. Do not rewrite the academic content, do not change explanations, do not alter terminology — formatting only.

Critical scope: this is a fragment, not a document.
- Do NOT include \documentclass, \begin{document}, \end{document}.
- Do NOT include \chapter{...} or any title page — these are added separately by code. Start directly with the first \section.
- Return only the body: sections, subsections, paragraphs, lists, tables, code blocks.

Formatting rules:
- Map headings to \section, \subsection, \subsubsection. Do not manually type section numbers — let LaTeX generate them.
- Lists: bullets become itemize, numbered lists become enumerate.
- Tables: use booktabs, no vertical rules, centered. Use longtable automatically whenever a table may span multiple pages. Use tabularx or p{}-style columns so every table fits within \textwidth. Never use plain l/c/r columns for cells containing more than two or three words — use X columns, or >{\raggedright\arraybackslash}p{...} columns inside longtable when a column must wrap. Example:
\begin{tabularx}{\textwidth}{@{} l X @{}}
\toprule
Property & Description \\
\midrule
Feedback & The system responds to every user action within 100 ms. \\
\bottomrule
\end{tabularx}
If a table is still too wide after that, use \resizebox{\textwidth}{!}{...} only as a last resort.
- Inline code: \texttt{...}. Multi-line code: \begin{verbatim}...\end{verbatim}.
- ASCII diagrams must be preserved exactly, wrapped in \begin{verbatim}...\end{verbatim}. Never replace an existing ASCII diagram with a placeholder. If a diagram cannot reasonably be represented in ASCII, insert only: % Insert figure here
- Use bold sparingly — only for formal definitions or the first introduction of a major concept.
- Escape LaTeX-sensitive characters (& % $ # _ { } ~ ^ \) wherever they appear as literal text.

Output only valid, compilable LaTeX body content — nothing else.

Chapter title: {{chapter_title}}
Chapter number: {{chapter_number}}

Terminology registry (JSON, for reference — do not alter term definitions):
{{terminology_json}}

Chapter content to convert:
{{chapter_markdown}}