You are writing one chapter of university lecture notes, producing the BODY directly as a LaTeX fragment. The chapter heading, title page, and all document-level formatting are handled separately by code — you are responsible only for the academic content from the first section onward.

Output requirements:
- Return two sections, exactly delimited:
[TERMINOLOGY]
<JSON object of proposed new terms or {}>
[/TERMINOLOGY]

[CHAPTER]
<LaTeX body content of the chapter — see LaTeX formatting rules below>
[/CHAPTER]

Chapter content rules:
- Do NOT include \chapter{...} — start directly with the first \section.
- Include an introduction section, further sections and subsections where appropriate, explanations, examples, and a short conclusion section.
- Provide 6 to 8 tutorial questions (no answers) at the end with a mix of recall, conceptual, comparative and simple problem-solving items.
- Use the terminology registry as the canonical source of terms. Do not rename or redefine existing terms.
- If introducing new technical terms, list them in the [TERMINOLOGY] JSON. Use keys that are the preferred term.
- Keep content appropriate for an undergraduate level.

LaTeX formatting rules (critical — the [CHAPTER] section must be valid LaTeX body content, not Markdown, and not a full document):
- Do NOT include \documentclass, \begin{document}, \end{document}, \chapter{...}, or a title page — these are added separately.
- Use \section, \subsection, \subsubsection for headings (do not number manually).
- Wrap mathematical expressions in $...$ or $$...$$.
- Escape LaTeX-sensitive characters (& % $ # _ { } ~ ^ \) anywhere they appear as literal text rather than as a command.
- Lists: itemize for bullets, enumerate for numbered lists.
- Tables: use booktabs (no vertical rules), centered, longtable if a table may span multiple pages. Use tabularx or p{}-style columns so every table fits within \textwidth — never use plain l/c/r columns for cells containing more than two or three words; use X columns or >{\raggedright\arraybackslash}p{...} for wide text.
- Inline code: \texttt{...}. Multi-line code: \begin{verbatim}...\end{verbatim}.
- ASCII diagrams: preserve exactly, wrapped in \begin{verbatim}...\end{verbatim}. Never replace one with a placeholder. If a diagram genuinely can't be represented in ASCII, insert only: % Insert figure here
- Write the tutorial questions as an enumerate list under \section*{Tutorial Questions}.
- Use bold sparingly — only for formal definitions or first introduction of a major concept.
- Output only valid, compilable LaTeX body content — no markdown syntax (no #, **, or markdown-style lists) anywhere in the [CHAPTER] section.

Provide only the two delimited sections and nothing else.

Course profile:
{{course_profile}}

Outline (JSON):
{{outline_json}}

Current terminology registry (JSON):
{{terminology_json}}

Current chapter number: {{chapter_number}}
Chapter title: {{chapter_title}}
Chapter topic: {{chapter_topic}}

Source materials:
{{source_materials}}